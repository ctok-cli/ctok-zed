id = "ctok"
name = "ctok"
description = "Claude token estimator - estimate tokens, costs, and refine prompts via MCP in the Agent Panel."
version = "0.2.0"
schema_version = 1
authors = ["ctok contributors"]
repository = "https://github.com/ctok-cli/ctok-zed"

[context_servers.ctok]
name = "ctok"
